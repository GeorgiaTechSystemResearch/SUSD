function z = quadratic(x)
    % quadratic test function with min at 20
    % - obj functions must take dxN matrices (each column is a d-dimension
    %   agent) and produce a 1xN row vector (each column is the evaluation)
    z = vecnorm(x-0);
end

